# Port for the Express backend (server/server.js).
# The Vite dev server (port 5173) proxies /api requests to this port.
PORT=3001

# Copy this file to .env and adjust as needed.
# Note: AI provider (9Router/OpenRouter) and YouTube OAuth credentials are
# NOT configured here — they're entered via the app's Settings tab and
# stored in server/data/database.json (gitignored).
